# URL do Cloudflare Worker que faz proxy do Gemini e da brapi.
# As chaves de API ficam como SECRETS no Worker, nunca aqui.
# Em dev local: http://127.0.0.1:8787  (wrangler dev)
# Em produção: https://smd-ai-proxy.<seu-subdominio>.workers.dev
VITE_WORKER_URL=
